# frozen_string_literal: true

require_relative './base'

module Warframe
  module Models
    # Model for <%= name.singularize.camelize %> Data {https://docs.warframestat.us/DOCUMENTATION_PATH /:platform/<%= name.camelize(:lower) %>>}
    # TODO: Replace above documentation path with correct one.
    class <%= name.singularize.camelize %>
      # TODO: Add attributes
    end
  end
end