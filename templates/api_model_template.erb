# frozen_string_literal: true

require_relative './base'

module Warframe
  module Models
    # Model for <%= name.singularize.camelize %> Data {https://api.warframestat.us/pc/<%= name.camelize(:lower) %>> /:platform/<%= name.camelize(:lower) %>}
    # TODO: Replace above documentation path with correct one.
    class <%= name.singularize.camelize %> < Warframe::Models::Base
      # TODO: Add attributes
    end
  end
end