# frozen_string_literal: true

require_relative './base'

module Warframe
  module Models
    # Model for <%= name.singularize.camelize %> data.
    # {https://api.warframestat.us/pc/<%= name.camelize(:lower) %> /:platform/<%= name.camelize(:lower) %>}
    # TODO: Replace above API path with correct one.
    class <%= name.singularize.camelize %> < <%= "Warframe::Models::Base" %>
      # TODO: Add attributes
    end
  end
end
